const q=s=>document.querySelector(s);const qa=s=>[...document.querySelectorAll(s)];const search=q('#toolSearch');if(search){search.addEventListener('input',e=>{const v=e.target.value.toLowerCase();qa('.tool-card').forEach(c=>{c.style.display=(c.dataset.name||'').includes(v)?'block':'none';});});}window.addEventListener('scroll',()=>{const h=document.documentElement;const p=(h.scrollTop/(h.scrollHeight-h.clientHeight))*100;const bar=q('.progress');if(bar)bar.style.width=p+'%';});